# ── Existing ML pipeline (do not remove) ────────────────────────────────────
streamlit==1.35.0
pandas==2.2.2
numpy==1.26.4
plotly==5.22.0
matplotlib==3.9.0
babel==2.15.0
scikit-learn==1.5.0
lightgbm==4.3.0
hmmlearn==0.3.2
joblib==1.4.2

# ── GoldGoal backend (Phase 0) ───────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1         # ASGI server; [standard] adds websockets + reload

# Database
sqlalchemy==2.0.30                # ORM; 2.x has cleaner session API than 1.x
alembic==1.13.1                   # Database migrations

# Validation
pydantic==2.7.1                   # Request/response schemas; v2 is significantly faster
pydantic[email]==2.7.1            # Adds email-validator for EmailStr fields

# Auth
python-jose[cryptography]==3.3.0  # JWT access tokens (Phase 1 auth)
passlib[bcrypt]==1.7.4            # Password hashing

# HTTP (for tests and for internal service calls)
httpx==0.27.0                     # Async-capable HTTP client; used by test fixtures

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7            # async test support
hypothesis==6.103.1               # property-based testing
